# Fruits_FullStacck_app
This Fruit Management App is a simple React-based CRUD project that lets users add, view, update, and delete fruit details. It uses JSON Server as a backend and includes search, validation, and toast notifications. The clean, responsive UI makes it easy to use on any device.
